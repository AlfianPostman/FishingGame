using UnityEngine;

public class SuperStateAction : PlayerStateManager
{
    public SuperStateAction(PlayerGeneral PLAYERGENERAL, PlayerStateMachine PLAYERSTATEMACHINE) : base(PLAYERGENERAL, PLAYERSTATEMACHINE)
    {
    }
}